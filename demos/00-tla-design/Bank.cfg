\* Two concurrent withdrawers, each taking 6 from a starting balance of 10.
\* Sequentially this is impossible (the second is refused). Concurrently, the
\* check-then-act race lets BOTH through: 10 -> 4 -> -2.
CONSTANTS
    Procs   = {p1, p2}
    Amount  = 6
    Initial = 10

SPECIFICATION Spec
INVARIANT NoOverdraft
